# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache
# Suffixed copies of the above, set aside while comparing two runs. The
# anchored names do not cover them, which is how a set of them reached a
# commit; a finished run left in the tree is debris under any name.
/test-results-*
/playwright-report-*

# next.js
/.next/
/out/
# Build output set aside while comparing two builds, for the same reason.
/.next-*
/.swc
next-env.d.ts

# production
/build

# typescript
*.tsbuildinfo

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — deny-all, then allow only the committable example.
# This repo is PUBLIC and .env here holds a GITHUB_TOKEN (repo, workflow) plus
# dashboard credentials. The previous rules only covered .env.local and friends,
# which left a bare `.env` committable.
.env
.env.*
!.env.example

# vercel
.vercel

# aiur daemon runtime state — rewritten continuously by the live daemon
.aiur/model-usage.json
